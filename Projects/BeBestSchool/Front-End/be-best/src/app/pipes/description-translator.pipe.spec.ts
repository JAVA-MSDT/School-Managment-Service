import { DescriptionTranslatorPipe } from './description-translator.pipe';

describe('DescriptionTranslatorPipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionTranslatorPipe();
    expect(pipe).toBeTruthy();
  });
});
